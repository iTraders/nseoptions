# Dashboard backend dependencies
# Kept separate from the core requirements.txt so the data module stays
# lightweight; install with: pip install -r requirements-dashboard.txt

fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.10.3
